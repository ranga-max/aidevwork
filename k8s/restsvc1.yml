apiVersion: v1
kind: Service
metadata:
  name: iceberg-rest
  labels:
    app: iceberg-rest
spec:
  type: ClusterIP
  ports:
    - protocol: TCP
      port: 8181
      targetPort: http
  selector:
    app: iceberg-rest
