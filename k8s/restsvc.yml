apiVersion: v1
kind: Service
metadata:
  name: rest
spec:
  selector:
    app: iceberg-rest-catalog
  ports:
    - protocol: TCP
      port: 8181
      targetPort: 8181
  type: ClusterIP # Use LoadBalancer if external access is required
